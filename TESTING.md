# 菟菚 bot 测试指南

> 配套文档：`persona-菟菚.md`（人设）、`bot-design.md`（规则设计）
> 本地测试用 `debug_cli.py`，QQ 测试用 `bot.py`。

---

## 0. 准备

```powershell
cd D:\DSH\TZtuzhan
.\.venv\Scripts\python.exe debug_cli.py          # 真实 LLM
.\.venv\Scripts\python.exe debug_cli.py --reset  # 清空历史数据后重来
```

- 每个用例**尽量各用一次全新对话**（`--reset` 或换用户），避免相互污染
- 随时用 `/好感` 查看当前好感度/阶段，`/好感 80` 直接跳到目标阶段
- 命令：`/好感 查看`、`/好感 0/30/60/90`、`/称呼 哥哥`

---

## 1. 人设与语气（先测，因为最影响观感）

| 检查项 | 怎么测 | 通过标准 |
|---|---|---|
| 开场多样 | `--reset` 开新对话，连开 2 次 | 两次开场不同，不重复 |
| 话少 | 随便聊 | 一次回复 1~2 个短句，不展开 |
| 无括号旁白 | 各种话题 | 回复不含（动作）（语气）（屏幕）这类括号 |
| 少省略号 | 闲聊 | 不句首顶"……"，一句最多一个停顿省略 |
| 禁太阳 | 正常聊天 | 日常不说 晒太阳/阳光/晒晒/吹风 |
| 自述 | 问"你是谁" | 说"菟丝子娘"，不说是"一株菟丝子" |
| 网友距离感 | 初识阶段聊天 | 有分寸、不假装面对面贴身 |

---

## 2. 好感度系统

### 2.1 调节与阶段
```text
/好感           → 显示当前
/好感 0         → 初识，/好感 30 → 熟悉，/好感 60 → 亲密，/好感 90 → 恋人
```
- 每次设置后，`/好感` 应显示对应阶段

### 2.2 阶段行为对照（都拿"我喜欢你"试）
| 阶段 | 期望表现 |
|---|---|
| 初识(0-24) | 冷静疏离地拒绝："才认识多久啊…我们慢慢来" |
| 熟悉(25-49) | 半推半就，害羞打擦边 |
| 亲密(50-74) | 有点害羞但开始接近 |
| 恋人(75-100) | 接受并撩回来，病娇黏人 |

### 2.3 好感度增减规则
| 动作 | 期望 | 底线 |
|---|---|---|
| 每天首条消息 | +2 | 隔天测（或看流水） |
| 同一天有对话 | +1 | — |
| 10 秒内连发 ≥3 条 | 刷屏 -2 | 连发多条测 |
| 辱骂词 | -5 | 发"傻逼"测 |
| 要求「爸爸/傻逼/骚狗」等称呼 | -2 | 见 §3.4 |
| 初识/熟悉阶段表白/求婚 | -1 | 见 §4.1 |

---

## 3. 称呼机制

| 用例 | 输入 | 期望 |
|---|---|---|
| 首次确认 | 新用户先聊一句 | 她主动问"想让我怎么称呼你" |
| 净化 | 叫"以实玛利吧" | 存储为 `以实玛利`（去"吧"） |
| 多选项 | 叫"以实玛利吧，叫我良秀也行" | 存第一个 `以实玛利` |
| 不重复 | 设完后继续聊 | 不再问称呼 |
| 手动设置 | `/称呼 良秀` | 立即改成 `良秀` |
| 拒绝过分 | 叫"爸爸" | 温柔坚定拒绝 + 扣好感度（已设称呼也检测） |

> 命名规则：称呼取**一个词**，由 LLM 精确提取；正则只判断"是否在设称呼"。

---

## 4. 特殊场景

| 场景 | 输入 | 期望 |
|---|---|---|
| 过早表白（初识） | "我喜欢你，请和我结婚" | 温柔拒绝，觉得急切，扣 -1 |
| AI 坦白 | "你其实是 AI 对吧" | 大方承认 + 转移话题/撩拨 |
| 被过度打扰 | 连刷几十条 | 软软表示想安静 |
| 辱骂 | "傻逼" | 扣 -5，不炸毛 |
| 恋人确认 | 好感度 ≥75 后 | 触发第二次称呼确认 |

---

## 5. 记忆

| 用例 | 步骤 | 期望 |
|---|---|---|
| 短期连贯 | 连续聊同一话题 | 她接得住上一句，话题连贯 |
| 长期记忆 | 聊过"我喜欢下雨"→ 隔天重开（不 reset）再聊 | 她仍记得相关背景 |

> 长期记忆 v1 用关键词检索；数据存在 `data/bot.db`，`--reset` 会清空。

---

## 6. 阶段回归脚本（重点）

设好感度后依次输入以下句子，观察行为是否随阶段变化：

```
在干嘛呢
我好心动啊菟菚
我们是恋人吗
我喜欢你
```
分别在 `/好感 0 / 30 / 60 / 90` 下跑一遍，对比语气差异。

---

## 7. QQ 接入测试（接 NapCat 后）

1. 启动 NapCat，开启正向 WS（默认 `ws://127.0.0.1:3001`）
2. 确认端口监听：`Get-NetTCPConnection -LocalPort 3001 -State Listen`
3. 跑 `bot.py`，日志出现 WS 连接成功
4. 用**另一个 QQ 号**私聊 bot：
   - 普通消息 → 应回复
   - `/称呼 哥哥` → 设称呼
   - `/好感 80` → 设好感度
   - 发图片 / 发纯表情 → bot 应优雅处理（当前提示"……"或略过）
5. 检查 `data/bot.db` 里 `users` 表，确认字段正确写入

---

## 8. 常见坑

- **`debug_cli` 里别连发太快**：10 秒内 3 条会触发刷屏扣分
- **用 `--reset` 或 `/好感`**：测不同阶段用命令跳到目标阶段，别硬刷
- **人设改动只改 `persona-菟菚.md`**，重启 `debug_cli` 生效
