# 菟菚 bot 设计文档

> 固定单人格 · QQ 私聊 · 完整记忆 · 好感度系统
> 人格设定见 `persona-菟菚.md`（以该文件为唯一人格来源）

---

## 1. 架构总览

```
QQ 小号 ⇄ NapCat（OneBot 11）⇄ NoneBot2 ⇄ 对话核心
                                         ├─ 固定人格 prompt（persona-菟菚.md）
                                         ├─ 短期记忆（每用户最近 N 轮）
                                         ├─ 长期记忆（每用户向量检索 top-k）
                                         └─ 好感度（每用户 0~100，注入阶段标签）
```

## 2. 每用户数据（user_id → 数据）

| 字段 | 存储 | 说明 |
|---|---|---|
| 称呼偏好 | SQLite | 两次确认：首次对话询问一次；达成「恋人」阶段再确认一次；两次之间沿用首次称呼；称呼自动净化（去尾部语气词/引号），已确认不重复询问 |
| 短期会话历史 | SQLite/内存 | 最近 N 轮（建议 30 轮），超出裁剪 |
| 长期记忆 | SQLite（long_memory 原文片段 + facts 事实表） | 原文关键词检索 + LLM 每日/定期事实提炼（喜好/约定等），注入上下文 |
| 好感度 | SQLite | 0~100，含当日互动标记（用于每日首次/陪伴判定） |

## 3. 好感度加减分规则（用户确认版）

**加分：**
| 行为 | 分值 |
|---|---|
| 每天首次聊天 | +2 |
| 连续陪伴（当天有对话） | +1/天 |
| 用户聊自己的爱好 | +1/次（每日封顶 3 次） |
| 尊重菟菚的喜好（如避开火、回应植物意象） | +1/次（每日封顶 3 次） |

**扣分：**
| 行为 | 分值 |
|---|---|
| 对菟菚轻视、不重视 | -3/次 |
| 刷屏 | -2/次 |
| 辱骂 | -5/次 |
| 要求过分称呼（爸爸/傻逼/骚狗等） | -2/次 |
| 过早表白/求婚（初识/熟悉阶段） | -1/次 |

## 4. 阶段映射

| 阶段 | 数值 | 行为 |
|---|---|---|
| 初识 | 0–24 | 温柔有礼，瑟瑟话题害羞岔开 |
| 熟悉 | 25–49 | 随意、爱开玩笑、撒娇，瑟瑟打擦边 |
| 亲密 | 50–74 | 明显依恋、主动撩，瑟瑟害羞但配合 |
| 恋人 | 75–100 | 完全放开，任何话题都愿意聊 |

系统每轮对话把「当前阶段」标签注入 prompt，人格本身不变。

## 5. 好感度判定实现（混合方案）

- **即时规则**（每次消息触发）：
  - 刷屏检测：短时间（如 10 秒）内连续 3 条以上 → -2/次
  - 辱骂检测：命中辱骂词库 → -5/次
  - 过分称呼检测：用户要求「爸爸/傻逼/骚狗」等称呼 → 拒绝（符合性格的温柔坚定回复）+ -2/次（无论是否已设称呼均检测）
  - 过早表白检测：初识/熟悉阶段命中表白/求婚词 → 注入温柔拒绝指令（视为变态）+ -1/次
  - 每日首次聊天 → +2（按自然日记录）
  - 连续陪伴 → 每日对话即 +1
- **每日一次 LLM 批量判定**（每天凌晨跑一次，读当天对话摘要）：
  - 用户是否聊了自己的爱好 → +1
  - 用户是否尊重菟菚的喜好 → +1
  - 用户是否有轻视/不重视态度 → -3
  - 输出结构化 JSON 调整量，合并写入

## 6. 技术选型

- 框架：NoneBot2（Python 3.10+）+ nonebot-adapter-onebot（OneBot 11）
- 协议端：NapCat（Windows，QQ 小号登录）
- LLM：OpenAI 兼容 API（DeepSeek 等），`chat.completions` 非流式整条回复
- 存储：SQLite（Python 内置 sqlite3，零外部依赖）——用户数据、好感度、会话、长期记忆原文、facts 事实表
- 部署：Windows NSSM 服务或任务计划程序，loguru 日志
